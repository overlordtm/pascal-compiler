program izpit2;

const t = true;
      f = false;
      a = 3;
      b = 4;

var   x : boolean;
      xx: integer;

begin
    putch('a');
    putch(chr(10));
    
    {xx := a*b;
    
    putint(xx);
    putch(chr(10));
    }
    x := xx*false;
    
    if x = true then
        putch('y')
    else
        putch('n');

    putch(chr(10));
    putch('b');
    putch(chr(10));
end.